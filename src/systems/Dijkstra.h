#ifndef DIJKSTRA_H
#define DIJKSTRA_H

// Returns shortest steps from (startX,startY) to (exitX,exitY), or -1 if unreachable.
int findShortestPath(int startX, int startY, int gridRows, int gridCols, int exitX, int exitY);

#endif
